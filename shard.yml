name: fast_irc
version: 0.3.4

crystal: ">= 0.35.1, < 2.0.0"

authors:
  - Stephanie Wilde-Hobbs <git@stephanie.is>

description: |
  A fast IRC parsing library for crystal  

targets:
  parse-benchmark:
    main: benchmark/parse.cr
  serialize-benchmark:
    main: benchmark/serialize.cr

license: MIT
